---
# vars file for Ubuntu 16

# The packages to install
package_names:
  - curl
  - openvpn
  # Ubuntu 16 only supports python2
  - python-ldap

# The name of the OpenVPN service
service_name: openvpn
